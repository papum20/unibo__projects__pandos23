

cambiamenti phase1:
	pcb : inizializzazioni campi non usati in phase1

		/*
NOTES:
1.	N_INTERRUPT_LINES or N_INTERRUPT_LINES*N_DEV_PER_IL ?
2.	"two semaphores for each terminal device":
	does it mean 2*2, or 2, of which ONE for each terminal?
3.	IL_IPI: what is it? Does it count?
*/

LDIT takes ms?


ns: non aggiunge ns se già presente uno del tipo?


EXCEPTIONS:
syscall cast tipi
- syscall macro
- 3 tipi return
- impl. pid
- syscall doio: 
	-	uso le struct di umps
	-	assumo che con i terminali comunque si passino 4word di args, ma quella non usata sia =0 (come dice il libro,
		se si usano solo 2 arg con i terminal perché si fa r/w)
-	device identificato da suo addr (cmdAddr)


SEMAFORI:
diventano anche negativi,
così abs(s_key) = n proc blocked
//decidere comportamento semafori (binari)
( the value of a semaphore is negative, it is an invariant that the abso-
lute value of the semaphore equal the number of pcb’s blocked on that
semaphore. Hence if a terminated process is blocked on a semaphore,
the value of the semaphore must be adjusted; i.e. incremented.
)

NS:
si deve cambiare la visita in ampiezza, perché interferisce con readyQ ed eventuali
altre code.
per ora in TERMINATE_PROC uso la ricorsione; se si vuole fare con interazione, basta creare
una struct aggiuntiva
struct iterator {
	list_head list;
	pcb_t *p;
}
